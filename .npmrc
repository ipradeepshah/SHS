# ── Siyaram Hardware — npm config ──────────────────────────────
# Prevents "npm audit fix --force" from downgrading Next.js
fund=false
audit=false
save-exact=false

# DO NOT run "npm audit fix --force" — it breaks Next.js version
